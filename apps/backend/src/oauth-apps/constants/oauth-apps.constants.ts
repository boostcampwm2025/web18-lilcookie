/**
 * OAuth Apps 관련 상수
 */

/** 사용자당 최대 OAuth App 생성 개수 */
export const MAX_OAUTH_APPS_PER_USER = 5;

/** 기본 OAuth 스코프 */
export const DEFAULT_SCOPES =
  "openid profile offline_access email links:read links:write folders:read folders:write ai:use";
