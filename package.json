{
  "name": "web18-lilcookie",
  "version": "1.0.0",
  "description": "URL 원클릭 저장 기획 사용자 테스트를 위한 프로토타입",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "extension",
    "extension-wxt"
  ],
  "scripts": {
    "start:dev": "concurrently \"npm run -w backend start:dev\" \"npm run -w frontend dev\" \"npm run -w extension-wxt dev\"",
    "build": "npm run build -w frontend && npm run build -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
